[server]
headless = true
address = "0.0.0.0"
port = 5000

[theme]
primaryColor = "#FF6B00"  # Warm Garfield Orange
backgroundColor = "#1A1A1A"  # Dark background
secondaryBackgroundColor = "#252525"  # Darker cards
textColor = "#E1E1E1"  # Light text for contrast
font = "Inter"